{% extends "base.html.twig" %} 

{% block body %}
{% include "header/header.html.twig" %} 
<h1>Mon compte</h1>
<table>
    <tbody>
        <tr>
            <th>Email :</th>
            <td>{{user.email}}</td>
        </tr>
        <tr>
            <th>Nom :</th>
            <td>{{user.lastName}}</td>
        </tr>
        <tr>
            <th>Prénom :</th>
            <td>{{user.firstName}}</td>
        </tr
        {% for section in section %}
        <tr>
            <th>Section :</th>
            <td>{{section.label}}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>
<a href="{{path('app_user_edit', {'id': user.id})}}" class="btn btn-lg btn-primary">Modifier</a>
{% endblock %}